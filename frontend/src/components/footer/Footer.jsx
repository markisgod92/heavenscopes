import { Container } from "react-bootstrap"

export const Footer = () => {
    return (
        <footer className="fixed-bottom bg-secondary">
            <Container fluid>
                <div>FOOTER</div>
            </Container>
        </footer>
    )
}